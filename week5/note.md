binary heap, array, sorted array (datastructure) -> priority queue interface (which has insertion, extraction operation) -> sorting algorithms(that take different time complexity based on different datastruucture under interface)

max heap is the best datastructure to implement priority queue

building max heap from using max_heapify_up -> O(n log n)
building max heap from using max_heapify_down -> O(n)